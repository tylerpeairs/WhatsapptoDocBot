1. Collect token counts per user
2. Build test directories and cases, Use chatgpt to generate more prompt engingeering & function Tests, automated testing suite, add embeddings/fine-tun for classification
3. Application Security Review
4. Error collection & handling for the user, collect document_contents, and update_requests in order to fine-tune/embed
5. get recipient whatsapp id from webhook instead of storing it as env variable, at this point see if you can do the opt-in by contacting the bot



Continue to prompt engineer for 10 edge cases.




last 10 examples as 10-hot
User feedback
User added categorization
Delete
Can use text generation for list of categories then add that to a zero-shot classifier






Nice to have:
Tests for longer text strings
Add speach-to-text processing
salesforce & notion & airtable connections
assistant model with message threads for categorization recommendations