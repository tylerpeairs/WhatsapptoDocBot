
4. Play with doc api to figure out how to manipulate.
5. set up agent to manipulate doc api with message content
6. Look into application security
7. refresh the auth token and check if it's expired based on functions below


everytime a new request is made after expiration (time function), then obtain a new access token by querying the database for the refresh token. Likely diffcheck 60 minutes between webhooks
keep auth token in session, but also store it in db

get recipient whatsapp id from webhook instead of storing it as env variable, at this point see if you can do the opt-in by contacting the bot
add unit tests with copolit


#Figured out what's wrong with Oauth state - create a development environment on nonNGROK webserver


Login success message & store user in database
"""


def is_token_expired(credentials):
    # Assuming you know the lifespan of your tokens, e.g., 1 hour
    token_lifespan = timedelta(hours=1)
    now = datetime.utcnow()
    # Calculate the age of the token
    token_age = now - credentials['updated_at']
    return token_age > token_lifespan

    """